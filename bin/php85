#!/bin/bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

docker run --rm -it \
    -v $(pwd):/app \
    -v "$SCRIPT_DIR/../php/8.5/php.ini:/usr/local/etc/php/conf.d/custom.ini" \
    -w /app \
    --network dev-net \
    php:8.5-dev \
    php "$@"
